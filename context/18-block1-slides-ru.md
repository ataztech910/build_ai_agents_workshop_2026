# Block 1 — Your First Agent — сценарий слайдов (RU)

Черновик содержания слайдов для Block 1 (25 мин по `context/02-workshop-plan.md`):
7 мин live-coding (hello-agent → instruction-эксперимент → FunctionTool),
18 мин участники пишут сами (задания 1.1 и 1.2 из `tasks/01-adk.md`).

Это НЕ вступление (там уже есть теория агента/ADK/план, 13 слайдов) — это
продолжение колоды сразу после слайда 13 ("Let's build the first one").
Sequential (1.3, Block 2) и Parallel (1.4, бонус) сюда не входят — другие
блоки, другой тайминг.

---

## Слайд 14 — Section: Block 1

Заголовок: "Block 1: Your First Agent". Подзаголовок: живой код 7 мин,
дальше участники пишут сами 18 мин.

## Слайд 15 — Что строим (recipe)

Три шага, ровно то, что реально в `hello-agent.ts`:
1. Создать `LlmAgent` (name, model, instruction)
2. Задать вопрос (`newMessage`)
3. Запустить через `Runner` и вывести ответ

Показываем как список — участники увидят его же в файле, когда откроют.

## Слайд 16 — "instruction — контракт, не подсказка"

Ключевая фраза блока (дословно из `02-workshop-plan.md`). Демонстрация
вживую: агент С инструкцией → агент БЕЗ инструкции, тот же вопрос — разное
поведение. На слайде — просто эта фраза крупно + короткое пояснение: без
инструкции модель ведёт себя непредсказуемо от запуска к запуску, потому что
у неё нет зафиксированной роли.

## Слайд 17 — Два варианта модели

В `hello-agent.ts` (и во всех файлах Block 1) — тумблер прямо в коде:
```ts
const model = "gemini-flash-latest"; // ADK default — needs GOOGLE_GENAI_API_KEY in .env
// const model = new KitanaLlm({ model: "auto", models: { ollama: process.env.OLLAMA_MODEL } });
```
Просто закомментировать/раскомментировать нужную строку — никаких других
правок в коде. Показать это явно, чтобы участники с Kitana не потерялись.

## Слайд 18 — FunctionTool: даём агенту инструмент

Диаграмма (переиспользуем стиль agent loop из вступления, но конкретно):
User → "какая погода в Москве?" → Model решает вызвать getWeather →
Tool исполняется → результат обратно модели → финальный ответ.
Плюс список: `FunctionTool({ name, description, parameters, execute })`.

## Слайд 19 — Теперь пишете сами (18 мин)

- Задания 1.1 и 1.2 из `tasks/01-adk.md`
- Скелет: `examples/01-adk/starter/{hello-agent,tool-agent}.ts`
- Застрял >5 мин? → `examples/01-adk/solution/`
- Подсказка для зависших: "Что возвращает инструмент? Покажи console.log
  перед return."
